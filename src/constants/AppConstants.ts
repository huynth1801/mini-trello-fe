class AppConstants {
    static HOME_PATH = "http://localhost:3000"
    static API_PATH = "http://localhost:8080"
    static BOARD_PATH = this.API_PATH + "/boards"
    static ACCESS_TOKEN = "accessToken"
    static SELECTED_BOARD_ID = "selectedBoardId"
}

export default AppConstants